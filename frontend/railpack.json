{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "cd frontend && npm install"
      ]
    },
    "build": {
      "commands": [
        "cd frontend && npm run build && rm -rf ../dist && cp -r dist ../dist"
      ]
    }
  }
}
