{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "true"
      ]
    },
    "build": {
      "commands": [
        "if [ -f package.json ]; then rm -rf node_modules && npm ci --include=dev && npm run build; elif [ -f frontend/package.json ]; then cd frontend && rm -rf node_modules && npm ci --include=dev && npm run build && rm -rf ../dist && cp -r dist ../dist; fi"
      ]
    }
  }
}
