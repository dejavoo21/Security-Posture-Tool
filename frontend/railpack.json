{
  "$schema": "https://schema.railpack.com",
  "steps": {
    "install": {
      "commands": [
        "if [ -f package.json ]; then npm install --include=dev; elif [ -f frontend/package.json ]; then cd frontend && npm install --include=dev; fi"
      ]
    },
    "build": {
      "commands": [
        "if [ -f package.json ]; then npm run build; elif [ -f frontend/package.json ]; then cd frontend && npm run build && rm -rf ../dist && cp -r dist ../dist; fi"
      ]
    }
  }
}
